{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "start": "PROCESS_NAME='GStreamer' SERVER_PORT=3000 node src/server",
    "docker:build": "docker build -t server .",
    "docker:start": "docker run -it --network host server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "mediasoup": "^3.10.5",
    "shelljs": "^0.8.5",
    "uuid": "^8.3.2",
    "ws": "^8.4.2"
  }
}
